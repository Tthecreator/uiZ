/// @description uiz_back_resumeSettings()
function uiz_back_resumeSettings() {
	//sets the current background settings as determined using uiz_back_captureSettings().
	//this is needed when drawing multiple sub backgrounds in a single objects. (most often using prefixes like _item_ or _topbar_)
	//use uiz_back_captureSettings() to get the captures settings.
	bkspr=bkspr_old;
	bkcol=bkcol_old;
	bkmar=bkmar_old;
	texas=texas_old;



}
