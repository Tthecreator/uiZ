/// @description uiz_generateRandomDigits(number of digits)
/// @param number of digits
function uiz_generateRandomDigits(argument0) {
	/*Will return a real random number with the maximum length of the specified number of digits. */
	return irandom(power(10,argument0)-1);



}
