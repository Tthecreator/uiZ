/// @description uiz_dpiToPx(dp)
/// @param dp
function uiz_dpiToPx(argument0) {
	//transforms dp values to pixels;
	/*
	These two scripts transforms dp values to pixel values.
	pxtodpx and pxtodpy are the same
	*/
	return argument0*uiz_dp;



}
